# Optional. CoinGecko Demo (free) or Pro API key.
# Without a key the server uses the public endpoint (~30 req/min).
# With a key the server uses the Pro endpoint and authenticates requests,
# unlocking higher rate limits and extra endpoints (e.g. top gainers/losers).
COINGECKO_API_KEY=
